# 项目约定（防屎山小抄）

## 文章文件命名

推荐格式：

```text
YYYY-MM-DD-简短英文或拼音说明.md
```

示例：

- 好：`2026-09-15-first-handmade-article.md`
- 好：`2026-09-15-welcome.md`
- 不好：`first handmake article.md`

说明：日期在前便于排序；只用小写、数字、连字符；避免空格。

## 标签

当前允许的标签：

- `Vibe入门`
- `日摘`

新标签先在这里登记，再写进文章。

## 日摘（每日工作日志）

每天 0 点前后由 Cursor 定时助手（或当天对话里的助手）写一篇，**不要用本地脚本拼摘要**。

命名：`YYYY-MM-DD-daily-digest.md`  
标签：`日摘`  
正文用四张表：**任务 / 进展 / 问题 / 反思**。当天几乎没做事可跳过或只写「无记录」。

来源优先：当日 `git` 变动、`posts/` 新增、对话里明确做过的事。不要编造成果。

## 阅读 vs 写作

| 场景 | 用什么 |
|------|--------|
| 自己/别人阅读 | `site/` 静态页（可部署到 GitHub Pages） |
| 你日常写文章 | `python admin_server.py` → 本机网页编辑器 |
| 高级/批量改 | 直接改 `posts/*.md` |

本机编辑器只监听 `127.0.0.1`，不要把它暴露到公网。

## 元数据里的 AI 提炼

文章开头 `---` 之间的字段叫 front matter。  
`ai_takeaway` / `ai_points` / `ai_next` 存在这里：

- 和标题、标签一样是文件的一部分
- `build.py` 只负责读出来排版，不联网问 AI
- 改正文后若提炼过时，再更新这些字段（或让 Cursor 帮你改）

## 站点文案

改 `config.toml`，然后 `python build.py`（或在编辑器里保存触发构建）。
